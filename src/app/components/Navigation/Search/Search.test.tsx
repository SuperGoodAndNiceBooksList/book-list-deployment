import React from "react";
import "@testing-library/jest-dom";
import { render, screen, waitFor } from "@testing-library/react";
import { Search } from "./Search";

describe("Search .tsx",() => {
    it.todo("Search renders with label",() => {

    })

    it.todo("Search takes text and returns a list of results",() => {
        
    })

    it.todo("Search can be set to genre",() => {

    })

    it.todo("Search can be set to author",() => {
        
    })
})